# ==============================================
# DocAI-MCP 集中配置文件
# ==============================================

# --- 基础配置 ---
PROJECT_NAME=DocAI-MCP
VERSION=1.0.0
# 后端服务对外访问地址（前端调用）
BACKEND_PUBLIC_URL=http://localhost:8000

# --- 数据库 (PostgreSQL) ---
POSTGRES_USER=docai
POSTGRES_PASSWORD=""
POSTGRES_DB=docai
POSTGRES_SERVER=postgres
POSTGRES_PORT=5432

# --- 对象存储 (MinIO) ---
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=""
# 内部服务访问地址
MINIO_ENDPOINT=minio:9000
# 浏览器访问地址（用于生成预览链接等）
MINIO_PUBLIC_ENDPOINT=http://localhost:9000
MINIO_BUCKET_UPLOADS=uploads
MINIO_BUCKET_OUTPUTS=outputs

# --- 消息队列 (Redis) ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379/0

# --- OnlyOffice 文档服务 ---
# OnlyOffice 内部访问地址
ONLYOFFICE_API_URL=http://onlyoffice
# OnlyOffice 对外访问地址（前端JS加载地址）
ONLYOFFICE_PUBLIC_URL=http://localhost:8081
# JWT 密钥 (安全起见应修改)
ONLYOFFICE_JWT_SECRET=""
ONLYOFFICE_JWT_HEADER=Authorization

# --- MCP & AI 模型配置 ---
# AI 模型服务商 (openai / zhipu / claude)
# AI 模型服务商 (openai / zhipu / claude / nvidia)
AI_PROVIDER=nvidia
# NVIDIA OpenAI-compatible Chat Completions URL
AI_API_BASE_URL=https://integrate.api.nvidia.com/v1/chat/completions
# AI 模型 API Key (请在此处填入您的API Key)
AI_API_KEY=""
# 默认模型（前端可覆盖选择）
AI_MODEL_NAME=glm4.7

# --- 前端配置 ---
VITE_API_BASE_URL=/api/v1
